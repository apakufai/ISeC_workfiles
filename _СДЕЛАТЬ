- Добавить на html второй таймер на 1 минуту, который удалял бы данные с сервера. На удаление pdf оставить таймер с 1 минутой

- Сделать так, чтобы pass'ы и данные пользователя для обмена между клиентом и сервером добавлялись в БД, а не летели на сервер. Сделать для этих данных отдельную таблицу.

- Переделать отправление данных на сервер (пр. из "userId : XXXXXXXX" в "userId_XXXXXXXX : XXXXXXXX").
Также - со всеми остальными данными, которые не сохраняются в localStorrage.
Также - со всеми pass'ами

* Вот пример объекта, где значением является массив:
const userActivities = {
    userId1: ["login", "view", "logout"],
    userId2: ["login", "purchase"],
    userId3: ["login", "view", "comment", "logout"]
};


Переделать таймер с 10 секунд на минуту



- Сделать функцию удаления кл-зн из сессии по истечении минуты

- Проверить, нужен ли:
@app.route('/check_pdf', methods=['POST'])
def check_pdf():

- Выправить margin-top для окна на UDI.html

- Добавить генерацию таблицы в БД, если её нет


- Перегенерировать страницы pdf в InDesign.

- Переделать ширину окна справки

- Переделать текст на странице результатов

- Когда Аля вобьёт результаты в БД, пробить их через нейру, чтобы она сохранила их в формате sql-запроса. Файл txt с кодом сохранить в репозитории.

- Поставить git. Отправить через его консоль проект на Гитхаб.

- Вбросить в нейру запрос на проверую ошибок со стороны сервера. Вбить проверки в код, если потребуется.



- Сделать сопроводительное письмо

- Сделать кабинет админитсратора (посмотреть, как устроен алхимиятест)

- Досканально проверить работу кода при генерации PDF - все строки, цифры и графики.
* особое внимание уделить значению "разницы" на одной из страниц, где текст.
